node_modules/
.next/
.env*
!.env.example
*.log
.DS_Store

# Large binary reference material (example tender BOQs/drawings) — kept local, read by the pipeline, not versioned
reference-docs/

# Test fixtures (generated at runtime, not versioned)
tests/fixtures/

# Human-review seed drafts (generated by scripts/seed.ts draft, reviewed then persisted)
seed-draft-*.json

supabase/.temp/
next-env.d.ts
*.tsbuildinfo
seed-drafts/

# Pipeline outputs (priced BOQ .json/.xlsx generated by `npm run pipeline` / acceptance runs)
*-priced.json
*-priced.xlsx
*-priced-v*.json
*-priced-v*.xlsx
labs-*.json
labs-*.xlsx
# Scratch scripts (temp .mts one-offs for debugging)
_*.mts
